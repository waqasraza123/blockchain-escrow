{
  "name": "@blockchain-escrow/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "install:browsers": "playwright install --with-deps chromium",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "seed": "tsx seed/cli.ts seed",
    "reset": "tsx seed/cli.ts reset",
    "smoke": "playwright test tests/smoke",
    "regression": "playwright test tests/regression",
    "deploy-smoke": "playwright test tests/deploy",
    "visual": "playwright test tests/visual",
    "ui": "playwright test --ui"
  },
  "dependencies": {
    "@blockchain-escrow/db": "workspace:*",
    "@prisma/client": "^6.6.0",
    "@playwright/test": "^1.54.2",
    "viem": "^2.38.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
